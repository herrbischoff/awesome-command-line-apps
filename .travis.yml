language: ruby

rvm:
  - 2.2

before_script:
  - gem install awesome_bot

script:
  - awesome_bot -f README.md,README-zh.md --allow-dupe --allow-redirect --white-list btrfs.wiki.kernel.org,harding.motd.ca,webdav.org,openvpn.net,duply.net,vanheusden.com
